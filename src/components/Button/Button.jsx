/* eslint-disable react/prop-types */
import PrimaryButtons from "./ButtonStyles";

const Button = ({ children }) => {
  return <PrimaryButtons>{children}</PrimaryButtons>;
};

export default Button;
